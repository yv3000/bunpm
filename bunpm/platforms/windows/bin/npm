#!/usr/bin/env node
// Unix-style shebang for PowerShell/Git Bash compatibility
// Passes 'npm' as invokedAs so wrapper.js knows context
process.argv.splice(2, 0, 'npm');
require('../core/wrapper.js');
